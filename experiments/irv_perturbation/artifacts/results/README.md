# 严格次优路径实验结果

`routes/` 保存两个城市某一自然日的所有有效路径。每行包含城市、行程编号、日期、
匀速行程时长、替换顺序、CH 最短路距离、最短路坐标序列和受限次优路坐标序列。
没有满足 15% 绕行约束的候选时，`alternative` 为 `null`。

`final/` 保存：

- 每个城市在 5%、10%、15%、20% 替换比例下的逐区域 IRV 基线和差值 CSV；
- 各替换比例的全城汇总 CSV；
- NYC、Chicago IRV 差值热力图；
- Chicago Near West Side 重路由走廊图及道路使用统计。
